 <div class="container-fluid">

                    <!-- Page Heading -->
                   <div class="card shadow mb-4">
  <div class="card-body">
    <h5 class="font-weight-bold">Welcome back, <%= setting.heroSection.name %>!</h5>
    <p class="mb-0 text-muted">
      Use the sidebar to manage your portfolio, services, contact messages, and more.
    </p>
  </div>
</div>
                 <% if (success && success.length > 0) { %>
      <div class="alert alert-success" id="flash-success">
        <%= success[0] %>
      </div>
    <% } %>
    
    <% if (error && error.length > 0) { %>
      <div class="alert alert-danger" id="flash-error">
        <%= error[0] %>
      </div>
    <% } %>
                    <!-- Content Row -->
              
                    <!-- Content Row -->

                   

                    <!-- Content Row -->
               

                </div>